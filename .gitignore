# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
build/
next-env.d.ts

# env — never commit real secrets
.env
.env.local
.env.*.local

# databases (embedded dev db)
.dbdata/

# operator desk store (enquiries + trip overrides) — runtime data, not source
.data/

# testing
coverage/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
tsconfig.tsbuildinfo
*.tsbuildinfo

# logs
*.log

# merged source archive (280MB) — kept locally, not in git
game.zip

# headless screenshots used for design review
shots/
